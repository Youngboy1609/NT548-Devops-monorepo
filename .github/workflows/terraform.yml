name: terraform-placeholder

on:
  workflow_dispatch:
  push:
    paths:
      - 'infra/terraform/**'
      - '.github/workflows/terraform.yml'

jobs:
  terraform-plan:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "TODO: add terraform fmt, validate, test, and Checkov."
