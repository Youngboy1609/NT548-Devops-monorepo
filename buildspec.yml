version: 0.2

env:
  variables:
    LAB2_BUILDSPEC_TARGET: cloudformation

phases:
  build:
    commands:
      - echo "Use ci/codebuild/${LAB2_BUILDSPEC_TARGET}-buildspec.yml for Lab 2 CodeBuild projects."
      - echo "Default CodePipeline target is ci/codebuild/cloudformation-buildspec.yml."

artifacts:
  files:
    - "**/*"
