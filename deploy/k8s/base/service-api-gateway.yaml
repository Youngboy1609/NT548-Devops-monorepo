apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: nt548-lab2
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: nt548-lab2
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api-gateway
  ports:
    - name: http
      port: 3000
      targetPort: http
