apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: nt548-lab2
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: nt548-lab2
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: order-service
  ports:
    - name: http
      port: 3000
      targetPort: http
