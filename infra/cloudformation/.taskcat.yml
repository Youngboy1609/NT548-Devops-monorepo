project:
  name: nt548-lab2-cloudformation
  regions:
    - ap-southeast-1

# Use `taskcat lint` for CI. Person 2 can create a local, untracked
# deployment config when real key pair and AMI choices are finalized.
tests:
  main:
    template: templates/main.yaml
    parameters:
      AllowedSshCidr: 203.0.113.10/32
      KeyName: taskcat-placeholder-key
      ImageId: ami-03c3282f979a6a9b0
      InstanceType: t2.micro
      ProjectName: nt548
      EnvironmentName: dev
