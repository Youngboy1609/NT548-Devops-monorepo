# CloudFormation Skeleton

This area contains templates, nested stacks, parameter files, docs, and test placeholders for Assignments 01 and 02.

## TODO

- Finalize how the network stack and compute stack should be split.
- Add the cfn-lint, taskcat, and CodeBuild flow.
